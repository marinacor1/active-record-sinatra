require "bundler" #requires bundler gem
Bundler.require #require method will require all of your gems for you so you don't have to require them for you
#will not require anything in group development 

require "sinatra/activerecord/rake"
