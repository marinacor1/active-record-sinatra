#title active record matters. needs to be singular. name of model.

class Film < ActiveRecord::Base #inherits activerecord. now film class has connection to database. lots of methods.
  #active record has all and find(id) method
  

end
